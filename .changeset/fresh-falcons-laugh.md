---
"@glyphp/client": minor
---

Add attestation verification with SigstoreVerifier and SlsaVerifier. GlyphClient now supports requireAttestation policy ('none' | 'danger' | 'all') and custom attestationVerifiers to validate external trust attestations before tool execution.
