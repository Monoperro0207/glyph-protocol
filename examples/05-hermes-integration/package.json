{
  "name": "05-hermes-integration",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "description": "Integration test: Hermes Agent + DeepSeek-V4 Flash + Glyph Protocol (via the MCP bridge)",
  "scripts": {
    "server": "tsx server.ts",
    "bridge": "tsx bridge.ts"
  },
  "dependencies": {
    "@glyphp/server": "workspace:*",
    "@glyphp/client": "workspace:*",
    "@glyphp/adapter-mcp-server": "workspace:*",
    "better-sqlite3": "^12.10.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "tsx": "^4.11.0"
  }
}
